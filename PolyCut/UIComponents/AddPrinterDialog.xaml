<ui:ContentDialog
    x:Class="AddPrinterDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PolyCut"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Enter Printer Name"
    d:DesignHeight="450"
    d:DesignWidth="800"
    CloseButtonText="Close"
    DialogMaxWidth="750"
    PrimaryButtonText="Add Printer"
    mc:Ignorable="d">

    <ui:ContentDialog.Resources>
        <Style BasedOn="{StaticResource {x:Type ui:ContentDialog}}" TargetType="{x:Type local:AddPrinterDialog}" />
    </ui:ContentDialog.Resources>

    <Grid>
        <StackPanel Orientation="Horizontal">
            <Label
                Margin="0,0,10,0"
                VerticalAlignment="Center"
                Content="Printer Name:" />
            <TextBox
                x:Name="PrinterNameTextBox"
                Width="200"
                VerticalAlignment="Center"
                Text="{Binding PrinterName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
    </Grid>
</ui:ContentDialog>
