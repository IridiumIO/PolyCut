<UserControl x:Class="MainSidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:c="clr-namespace:ValueConverters;assembly=ValueConverters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:loc="clr-namespace:PolyCut.Localisation;assembly=PolyCut.Localisation"
             xmlns:local="clr-namespace:PolyCut"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:pc="clr-namespace:PolyCut.Core;assembly=PolyCut.Core"
             xmlns:polycutShared="clr-namespace:PolyCut.Shared;assembly=PolyCut.Shared"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             x:Name="MainSidebar"
             d:DataContext="{d:DesignInstance Type=local:SVGPageViewModel}"
             d:DesignHeight="800"
             d:DesignWidth="380"
             x:FieldModifier="public"
             mc:Ignorable="d">
    <Grid VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TabControl Margin="0 0 0 0" VerticalAlignment="Stretch" BorderBrush="Transparent">
             <TabControl.Template>
        <ControlTemplate TargetType="{x:Type TabControl}">
            <Grid KeyboardNavigation.TabNavigation="Local">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0"
                            IsItemsHost="True"
                            Orientation="Horizontal"
                            KeyboardNavigation.TabIndex="1"
                            Panel.ZIndex="1" />

                <Border Grid.Row="1"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    <ContentPresenter ContentSource="SelectedContent"
                                      Margin="{TemplateBinding Padding}"
                                      SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                </Border>

            </Grid>
        </ControlTemplate>
    </TabControl.Template>
            <TabItem IsSelected="True" Style="{StaticResource CustomTabStyle}">
                <TabItem.Header>
                    <StackPanel Margin="5 0 5 0" Orientation="Horizontal">
                        <ui:SymbolIcon Symbol="PuzzlePiece24" />
                        <TextBlock Margin="6 0 0 0" loc:L.Context="Drawing/shape elements" loc:L.Value="Elements" />
                        <!--  Visibility="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TabItem}, Converter={StaticResource BoolToVisibilityConverter}}" />  -->

                    </StackPanel>
                </TabItem.Header>
                <local:Tab_Elements x:Name="ElementsTab" x:FieldModifier="Public" />
            </TabItem>

            <TabItem Style="{StaticResource CustomTabStyle}">
                <TabItem.Header>
                    <StackPanel Margin="5 0 5 0" Orientation="Horizontal">
                        <ui:SymbolIcon Margin="0 0 6 0" Symbol="AlignLeft16" />
                        <TextBlock loc:L.Value="Properties" />
                        <!--  Visibility="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=TabItem}, Converter={StaticResource BoolToVisibilityConverter}}" />  -->

                    </StackPanel>
                </TabItem.Header>
                <local:Tab_ElementProperties />
            </TabItem>

            <TabItem Width="9"
                     Margin="0 0 0 0"
                     IsEnabled="False" IsHitTestVisible="False"
                     Style="{StaticResource CustomTabStyle}">
                <TabItem.Header>
                    <Separator Height="28" Background="{StaticResource CardBackground}" />
                </TabItem.Header>
            </TabItem>



            <TabItem Style="{StaticResource CustomTabStyle}">
                <TabItem.Header>
                    <StackPanel Margin="5 0" Orientation="Horizontal">
                        <ui:SymbolIcon Margin="0 0 6 0" Symbol="Options20" />
                        <TextBlock loc:L.Value="Tool Config" />

                    </StackPanel>
                </TabItem.Header>
                <local:Tab_Configure />
            </TabItem>



        </TabControl>

    </Grid>


</UserControl>
